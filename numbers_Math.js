const num1 = 100;
const num2 = 60;
let val;

// Simple math
val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

// Math Object
val = Math.PI;
val = Math.E;

val = Math.round(-2.8);
val = Math.ceil(-2.8);
val = Math.floor(-2.8);
val = Math.sqrt(64);
val = Math.abs(-2.8);
val = Math.pow(8, 2);
val = Math.min(-8, 2, 3, 5);
val = Math.max(-8, 2, 3, 5);

val = Math.random();
val = Math.floor(val * 20) + 1;

console.log(val);
